TGS PLATFORM — Booking flow complete (experiences + order write path +
processing state + rich confirmation)
=====================================================================
SUPERSEDES the earlier order-write-path zip — deploy this one instead; it has
everything. Drop over matching files. New files: ExperienceBooking.tsx,
lib/supabase/admin.ts, app/actions/submitBooking.ts.

** ENV VAR REQUIRED (Vercel, platform project) **
  SUPABASE_SERVICE_ROLE_KEY = <Supabase service-role key>  (server-only, NOT
  NEXT_PUBLIC). Without it, "Request to book" errors.

** DATABASE ALREADY LIVE **: the sync_order_status() trigger bug (ambiguous
"total") was fixed in Supabase. No SQL to run.

WHAT'S NEW SINCE THE ORDER-WRITE-PATH ZIP
  app/checkout/page.tsx —
    • On "Request to book": a stepped PROCESSING modal (Connecting with the
      venue -> Holding your places -> Preparing your request -> Confirming),
      modelled on the LE "We're confirming your booking" screen, in TGS brand.
      When Stripe goes live, add a "Processing your payment securely" step.
    • A RICH CONFIRMATION page (was a one-line panel): status + reference,
      print / (download / resend = stubs), a thank-you panel, the itemised
      booking with per-item held status, a held total, honest trust framing.
      Reads the real cart snapshot taken at submit.
  Honest by design: says "held / nothing charged" now; flips to "confirmed /
  paid" when payments go live. No points, no invented reviews.

THE WHOLE FLOW: experience/venue -> cart -> checkout (contact + terms) ->
processing modal -> submitBooking (server-priced, held order + bookings +
items) -> rich confirmation.

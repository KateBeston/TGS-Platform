TGS PLATFORM — Booking review page + cart hand-off (Stage 2 of the flow)
** Self-contained venue display + the new /booking page. Supersedes the
   stage1 zip. **

NEW
  • components/venue/BookingCart.tsx — the sidebar now saves a resolved snapshot
    of the booking to the browser (localStorage) as it changes, and its
    "Review booking" button navigates to /booking. (One venue per order: a new
    booking overwrites the last.)
  • app/booking/page.tsx (NEW) — the review / cart page. Reads that snapshot and
    lays out the whole order: arrival / departure / guests, the items grouped by
    type (Accommodation / Wellness services / Room extras) with prices, the
    estimated total, the terms links, and Edit booking (back to the venue).
    "Request to book" is present but inert — it becomes the checkout entry next.
  • app/globals.css — the .br-* review-page styles.

Still nothing touches the database or money. The cart lives only in the
browser; it becomes a real order at checkout.

TO TEST LIVE
  1. On a venue, open the sidebar, add a room and/or a service, set dates/guests.
  2. Click "Review booking" → lands on /booking showing the order.
  3. "Edit booking" returns to the venue with the cart intact.
  4. Empty cart → /booking shows a tidy empty state.

NEXT: checkout — guest details, Société membership upsell, then payment
(Stripe test mode, gated by Andrew + Jeremy before anything real).

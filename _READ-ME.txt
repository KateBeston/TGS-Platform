TGS PLATFORM — Holistic cart + checkout (built from your uploaded repo)
======================================================================
Drop these over the matching files. One NEW file: app/checkout/page.tsx.

CHANGED
  components/venue/BookingCart.tsx   — the sidebar now writes ONE holistic
      cart (tgs_cart), accumulating across venues and rehydrating when you
      return to a venue. Each saved item carries its image, sub-heading,
      quantity label, unit price and the venue's cancellation line.
  components/venue/RetreatVenue.tsx  — passes the venue hero image.
  components/venue/WellnessVenue.tsx — passes the venue hero image.
  app/booking/page.tsx  — REPLACED. Now the rich semi-nested CART page:
      venue-grouped blocks (venue image + nested item rows with thumbnails,
      sub-heading, quantity dropdown on the right that recomputes price live,
      Edit / Delete), past-due venues dim with Reschedule / Delete, sticky
      sidebar (total, per-venue breakdown, Checkout now / Continue browsing),
      trust row. Reads/writes tgs_cart.
  app/checkout/page.tsx  — NEW. "Checkout now" lands here: Review (from the
      holistic cart) + Who's going (primary-contact modal, saved to browser)
      + Cancellation protection STUB + Payment STUB (terms + inert Request to
      book) + sticky Summary.
  app/globals.css  — cart (.cart-*, .cv-*), modal (.ck-modal*/.ck-field*) and
      checkout (.ck-*) styles appended.

NOTE — this replaces the old single-venue review flow. The sidebar no longer
writes the old tgs_booking key; the cart lives in tgs_cart.

WHAT SHOWS vs WHAT'S PENDING DATA
  • Venue + room images: REAL (from your uploaded media).
  • "Free cancellation until X": renders from the venue's cancellation line.
    Until the cancellation facility is populated (the approved next build:
    policy_id FK + portal editor + seed defaults), this line is usually blank
    and the card simply omits it — no invented terms.
  • Promo was/now (−15%) and "only N left": NOT shown yet — both need a data
    mapping (promo→rate; live availability = Phase 2). Flagged, not faked.
  • Payment + cancellation protection are gated stubs (Andrew / Jeremy).

TEST: add rooms/services on a couple of venues → sidebar "Review booking" →
/booking shows the holistic cart; change a quantity (price updates); Delete;
let a venue's dates pass → it dims with Reschedule; Checkout now → /checkout.

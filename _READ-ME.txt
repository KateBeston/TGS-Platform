TGS PLATFORM — Dynamic accommodation subtitle
** Self-contained venue display. Supersedes the promo-badges zip. **

CHANGE
  lib/venue.ts adds roomSummary(): the accommodation subtitle is worked out from
  the rooms with correct singular/plural —
    "12 rooms across 2 types", "5 rooms across 6 types",
    "8 rooms" (single type), "1 room" (singular).
  RetreatVenue.tsx uses it in place of the old hardcoded "N rooms across M types".

Also includes the earlier venue-display work (promo badges, uploaded-media
images, room/hero carousel, room modal).

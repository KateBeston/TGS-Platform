TGS PLATFORM — Bookings save to accounts + My Bookings (definitive booking set)
===============================================================================
SUPERSEDES all prior booking zips (experience-booking, order-write-path,
booking-flow-complete). Deploy this one. Drop over matching files.

** ENV VAR (Vercel, platform) **: SUPABASE_SERVICE_ROLE_KEY (server-only).
** DATABASE ALREADY LIVE **: sync_order_status() trigger fix + two new functions
   (get_my_bookings, get_my_booking_detail, granted to authenticated). No SQL.

FIX (important): the write path was setting orders.wellness_guest_id to the auth
UUID, but that column is bigint -> wellness_guests.id. A LOGGED-IN checkout would
have errored. Now:
  • bookings attach to the account by EMAIL (the link the platform already uses
    in get_my_platform_activity): guest_email = the signed-in user's auth email.
  • wellness_guest_id resolves to the real wellness_guests row when one exists.
  • guests (not logged in) still work; their contact email is used.
  Every order/booking/item is timestamped (created_at) as before.

MY BOOKINGS (account -> Bookings tab):
  Richer cards now — venue, dates, guests, a status badge (Pending outlined /
  Confirmed gold), and "Ref TGS-XXXXX · N items · booked <date>" with the total.
  Enquiries in progress listed beneath. Data via get_my_bookings() (email-scoped,
  SECURITY DEFINER). get_my_booking_detail(id) is live and ready for a per-booking
  detail page (next).

STILL TO COME: individual booking detail page (function is ready); "Manage /
cancel" actions; and the Stripe payment step (gated).

TGS PLATFORM — Room gallery redesign (bottom-right carousel) + modal fix
** Supersedes all earlier room-modal / room-gallery zips — deploy this one. **

FILES (drop into the platform repo, overwrite):
  components/venue/RoomGallery.tsx   (REDESIGNED — bottom-right carousel)
  components/venue/RoomDetails.tsx    (modal via portal — the earlier fix)
  components/venue/Section.tsx        (room card wiring)
  app/globals.css                     (gallery + modal styles)

GALLERY CHANGE
  • Main image is clean — no arrows or overlays on it.
  • A small carousel floats in the bottom-right, inside the image: small square
    thumbnails with small arrows either side. No frame, no borders — just the
    squares (a soft shadow lifts them off the photo) and the arrows.
  • Arrows step through the images; the leftmost thumbnail is the next one up;
    clicking a thumbnail brings that image into the main view.
  • One image = no carousel; no images = the name placeholder.

Also includes the modal portal fix (modal renders on document.body so it isn't
clipped by the card). Nothing else changed.

Deploy: unzip → replace the files → commit + push → Vercel builds.

TGS PLATFORM — Promotional badges on cards + listing hero
** Self-contained venue display. Supersedes the read-uploaded-images zip. **

NEW THIS ROUND
  • Promotional badges (from venue_listings.promo_badges) render as pills:
      - on the venue CARD, overlaid top-left on the image (first badge gold).
      - on the listing HERO, beneath the location.
  • lib/venues.ts Card type now includes promo_badges; VenueCard.tsx and both
    venue components render them.

Also includes the earlier work (uploaded-media images, the room/hero carousel,
the room modal), so this is the complete current venue display.

DB already live: venue_cards view carries promo_badges.

TGS PLATFORM — Cancellation facility (platform side) + cart/checkout + tracking
================================================================================
CUMULATIVE zip: holistic cart + checkout + cart-event tracking + the new
cancellation label. Drop over matching files. New: app/checkout/page.tsx,
app/api/cart-event/route.ts.

** DATABASE ALREADY LIVE ** (project qvriwjipnradsbilepyd) — you run no SQL:
  • cancellation_rules gained policy_id (FK to cancellation_policies) + indexes.
  • The duplicate jsonb cancellation_policies.tiers was retired — the structured
    tiers in cancellation_rules (days_before_arrival + refund_percent) are now
    the single source of truth.
  • The 9 live venues were seeded a default policy + 3 tiers:
        100% refund until 30 days before, 50% until 14, 0% thereafter.
  • Anon can read ACTIVE policies + their tiers (terms are public-facing).

PLATFORM CHANGES
  lib/venue.ts  — loads each venue's active default policy + tiers and computes
      free_cancellation_days (the widest full-refund window). Exposed on the
      venue as free_cancellation_days + cancellation_wording.
  BookingCart.tsx + RetreatVenue/WellnessVenue.tsx — pass freeCancelDays; the
      cart slice carries it so the label stays accurate if dates change.
  app/booking/page.tsx + app/checkout/page.tsx — render a DATE-AWARE label:
      • with a stay date:  "Free cancellation until 15 Oct 2026" (arrival − N)
      • no date yet:       "Free cancellation up to 30 days before arrival"
      • inside the window: "Cancellation policy applies"
      • no policy:         nothing (never an invented claim)

STILL TO DO
  • PORTAL editor — the structured per-venue tier editor (add/reorder tiers,
    deposit refundability, wording) lives in the portal repo, which isn't in
    this zip. Send me the portal repo and I'll build it, or I'll hand you the
    component to paste in. This is what lets you change a venue's policy without SQL.
  • Default tiers (100/30, 50/14, 0/after) are seeded; adjust per venue once the
    editor exists.
  • Jeremy signs the consumer-facing wording before go-live.
